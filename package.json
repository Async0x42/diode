{
  "name": "diode",
  "scripts": {
    "dev": "pnpm recursive run dev",
    "build": "pnpm recursive run build",
    "lint": "pnpm recursive run lint",
    "lint:all:eslint": "pnpm recursive run lint:eslint"
  },
  "devDependencies": {
    "@mikro-orm/migrations": "^4.5.7",
    "date-fns": "^2.23.0",
    "windicss": "^3.1.6"
  }
}
